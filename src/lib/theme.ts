/**
 * @deprecated G6: import từ '@/theme' (`src/theme/*`) thay vì file này.
 * Giữ lại để code cũ không vỡ giữa chừng; sẽ xóa khi migrate xong.
 */
export { radius, spacing } from '../theme/spacing';
export {
  darkTheme,
  lightTheme,
  useAppTheme,
  type AppTheme,
  type AppThemeColors,
} from '../theme/theme';
